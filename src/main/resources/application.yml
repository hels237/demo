spring:
  datasource:
    url: jdbc:postgresql://container-demo:5432/demo
    username: hels
    password: hels
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
          format_sql: true